<?xml version="1.0" encoding="UTF-8"?>

<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
  <plugin-info>
    <description>${project.description}</description>
    <version>${project.version}</version>
    <vendor name="${project.organization.name}" url="${project.organization.url}"/>
    <param name="plugin-icon">images/pluginIcon.png</param>
    <param name="plugin-logo">images/pluginLogo.png</param>
  </plugin-info>

  <resource type="i18n" name="i18n" location="i18n"/>

  <repository-hook name="Right Hook" i18n-name-key="right-hook.name" key="right-hook" class="com.carolynvs.stash.plugin.right_hook.RightHook">
    <description key="right-hook.description">Chuck Norris has the only hook you'll ever need</description>
    <icon>images/hookIcon.png</icon>
  </repository-hook>
</atlassian-plugin>
